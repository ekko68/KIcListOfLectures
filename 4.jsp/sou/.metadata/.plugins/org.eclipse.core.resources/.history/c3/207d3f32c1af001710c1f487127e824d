package sp.aop;

import java.lang.reflect.Method;

import org.springframework.aop.MethodBeforeAdvice;

//Advice=>모든 클래스에 공통으로 사용할 기능(소스코드->메서드 중심)
//인터페이스 대신(OOP)에 Advice클래스를 작성해서 구현(AOP)->Aspect중심
//특정 메서드의 실행위치를 앞에서 실행->MethodBeforeAdvice을 상속

public class BeforeLogAdvice implements MethodBeforeAdvice {

	//1)스프링의 AOP(메서드 중심)->핵심클래스(target)의 메서드(ex 회원수정)
	//2)생성된 객체를 배열로 받아온다. 3)핵심클래스의 객체를 얻어온다.
	@Override
	public void before(Method method, Object[] args, Object target) throws Throwable {
		// TODO Auto-generated method stub

	}

}



