<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
 "HTTP://mybatis.org/dtd/mybatis-3-config.dtd">
 
<configuration>
 <!--접속시 불러올 테이블의 정보,필드명을 지정
     mapper resource="경로포함해서 불러올 테이블의 정보xml
     typeAliases태그 등록->별칭을 등록(패키지명이 긴자바파일명)
     type="패키지명....클래스명(DTO) alias="별칭명"
    -->
 
 <typeAliases>
    <typeAlias type="lee.BoardCommand"
                    alias="board" />
 </typeAliases>
 
 <mappers>
    <mapper resource="lee/Board.xml"/>
    <!-- 
    <mapper resource="lee/Sangpum.xml"/>
    <mapper resource="lee/Customer.xml"/>
    <mapper resource="lee/Manager.xml"/> 
    -->
 </mappers>
</configuration>
