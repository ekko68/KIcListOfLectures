3일차(Spring의 JDBC Programming 작성방법)

--------------------------------------------------------------------------
Spring DI 마무리->DB접속->Spring의 JDBC Programming(crud) 11장
---------------------------------------------------------------------------

<!-- spring8 new BookClient()-->
<bean name="bookClient" class="spring8.BookClient">
     <property name="prop">
         <props>
            <prop key="server">192.168.0.57</prop>
            <prop key="connectionTimeout">5000</prop>
         </props>
     </property>
</bean>
 --자바소스 코드(p81)--

          Properties pro=new Properties();
          pro.setProperty("server",192.168.0.57);
          pro.setProperty("connectionTimeout",5000);
          ...
----------------------------------------------------------------  
         <props>
            <prop key="server(키속성명)">192.168.0.57(키값을 설정)</prop>
            <prop key="connectionTimeout">5000</prop>
         </props>
-----------------------------------------------------------------
setProp()호출=>{connectionTimeout=5000, server=192.168.0.57}
book=>BookClient의 객체생성시 [prop={connectionTimeout=5000, server=192.168.0.57}]

<bean name="protocolHanderFactory" class="spring9.ProtocolHanderFactory">
     <property name="map">
         <map>
             <entry>
                 <key><value>soap</value></key>
                 <ref  bean="soapHandler" />
             </entry>
             <entry>
                 <key><value>rest</value></key>
                 <ref  bean="restHandler" />
             </entry>
         </map>
     </property>
</bean>
----------------------------------------------------------
자바클래스를 작성->new X->xml설정

정적메서드--><bean id=""  class="패키지명...참조클래스명"
                      factory-method="호출할 정적메서드명">

</bean>
-----------------------------------------------
p97
--------
class ErpClientFactory{
  private Propeties prop;
  ErpClientFactory(Properties prop){
           this.prop=prop;
   }
  public static ErpClientFactory instance(){}
}
=================================
추가
             type(자료형)
빈객체의 타입이나 이름을 이용해서 의존성 객체를 주입
                                                -------------------객체를 얻어와서 가져온다
=>****자동으로 설정****
               (멤버변수)
1.byType->속성의 타입과 같은 타입을 갖는 빈객체를 생성->가져올때 사용
                           xml에서 찾아서 가져온다.

2.byName->속성의 이름과 같은 이름을 갖는 빈객체를 생성->가져올때 사용
3.빈즈 클래스도 상속->자식클래스도 객체를 생성->가져올때 사용
============================================
 before
<bean name="systemMoniter" class="spring10.SystemMoniter">
     <property name="call">
        <ref bean="phoneCall" />
     </property>
</bean>

<bean name="phoneCall"  class="spring10.PhoneCall" />

=>byType방법을 이용하겠다.
-------------------------------------------------------------------
after
<bean name="systemMoniter" class="spring10.SystemMoniter"
           autowire="byType">
           =============>SystemMoniter클래스에서 PhoneCall의 타입을
                                            자동으로 찾아서 의존성 주입
</bean>

<bean name="PhoneCall"  class="spring10.PhoneCall" />
                      -----------
                       의존성객체의 자료형type이 일치한 것을 찾아서
-----------------------------------------------------------------------
setCall()호출(call)=>spring10.PhoneCall@e45f292
moniter=>spring10.SystemMoniter@ff5b51f

byType을 이용해서 객체를 얻어올때 주의할점

expected single matching bean but found 2: PhoneCall,PhoneCall2; nested exception is org.springframework.beans.factory.NoUniqueBeanDefinitionException: No qualifying bean of type [spring10.PhoneCall] is defined: expected single matching bean but found 2: PhoneCall,PhoneCall2
	at org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory.autowireByType(AbstractAutowireCapableBeanFactory.java:1307)

****
byType을 사용하는 경우에는 동일한 타입의 클래스가 한개이상 설정하면 안된다.
---------------------------------------------------------------------------------
//(2)byName
	private PhoneCall phonecall;//type과 이름을 같게 준다(멤버변수명)

	public void setPhonecall(PhoneCall phonecall) {
		this.phonecall = phonecall;
		System.out.println("setPhone()호출(phonecall)=>"+phonecall);
	}
	@Override
	public String toString() {
		// TODO Auto-generated method stub
		return "SystemMoniter[phonecall="+phonecall+"]";
	}

결론->byType, byName을 사용하든 Setter Method를 통해서 의존성객체를
         주입(Injection)->얻어온다.
--------------------------------------------------------------------------------
xml의 설정인 경우

<!-- (2) byName -->
<bean name="systemMoniter" class="spring10.SystemMoniter"
           autowire="byName">
</bean>

<bean name="phonecall"  class="spring10.PhoneCall" />
-------------------------------------------------------------------------------
<!-- (3) 자바에서 객체를 생성X(추상 클래스)
   스프링컨테이너->해당 자기자신의 빈객체를 생성하지 마라 옵션->abstract=true
    자신빈즈-> parent="부모빈즈의 id부여" ->오버라이딩 가능
      class 자식클래스명 extends 부모클래스명   
-->
<bean id="commonMoniter"  class="spring11.SystemMoniter"
         abstract="true">
   <property name="periodTime" value="10" />
   <property name="sender" ref="smsSender" />
</bean>

<bean id="doorMoniter"  parent="commonMoniter" />
<bean id="lobbyMoniter"  parent="commonMoniter" >
   <property name="periodTime"  value="30" />
</bean>

<bean id="roomMoniter"  parent="commonMoniter">
  <property name="periodTime"  value="20" />
</bean>

<bean id="smsSender" class="spring11.SmsSender" />
---------------------------------------------------------------------
//4.객체를 의존성 주입(추상클래스 객체로 형변환)
		SystemMoniter moniter=context.getBean("doorMoniter",SystemMoniter.class);
		System.out.println("moniter=>"+moniter);
		
		SystemMoniter moniter2=context.getBean("lobbyMoniter",SystemMoniter.class);
		System.out.println("moniter2=>"+moniter2);
		
		SystemMoniter moniter3=context.getBean("roomMoniter",SystemMoniter.class);
		System.out.println("moniter3=>"+moniter3);


setSender()호출됨=>spring11.SmsSender@1e127982
setSender()호출됨=>spring11.SmsSender@1e127982
setSender()호출됨=>spring11.SmsSender@1e127982
moniter=>Systemoniter[periodTime=10,sender=spring11.SmsSender@1e127982]
moniter2=>Systemoniter[periodTime=30,sender=spring11.SmsSender@1e127982]
moniter3=>Systemoniter[periodTime=20,sender=spring11.SmsSender@1e127982]
-----------------------------------------------------------------------------------

프로젝트->Spring(DB접속)->xml 설정->crud(11장)

SpringTest3(db)->도서관리->스프링 방식

1.pom.xml ->스프링+DB연동 라이브러리 다운(설정)+추가~

<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-context</artifactId>
			<version>4.2.5.RELEASE</version>
		</dependency>
		
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-expression</artifactId>
			<version>4.2.5.RELEASE</version>
		</dependency>
		
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-beans</artifactId>
			<version>4.2.5.RELEASE</version>
		</dependency>
		
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-jdbc</artifactId>
			<version>4.2.5.RELEASE</version>
		</dependency>
		
		<!-- dbcp(ConnectionPool) or mysql -->
		<dependency>
			<groupId>commons-dbcp</groupId>
			<artifactId>commons-dbcp</artifactId>
			<version>1.4</version>
		</dependency>
		
		<dependency>
			<groupId>mysql</groupId>
			<artifactId>mysql-connector-java</artifactId>
			<version>5.1.34</version>
		</dependency>
---------------------------------------------------------------
2.xml파일 설정->빈즈 설정->DB연동(DI)
  +

3.테이블 생성
===============================
/* student (age,name,id(autoincrement) */
create table student(
id int not null auto_increment,
name varchar(20) not null,
age int not null,
primary key(id));

desc student

=================================
3.자바 App~->DTO,DAO,main() 가진 클래스 실행
==================================
DBConnectionMgr.java===>MemberDAO

package studentdb;

//String,int->Integer
public class Student {

	private Integer age;//나이 
	private String name;//이름
	private Integer id;//학생번호
	,,,,

=========================
 업무에 따라서 인터페이스를 이용->추상메서드를 선언
  StudentDAO 인터페이스

 인터페이스를 상속받아서 DAO클래스->메서드 호출
                            StudentJDBCTemplate










